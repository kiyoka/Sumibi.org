
                             TODOリスト


    --- sumiyaki
       - 送りがなの結合処理をした後に、漢字のみで構成される単語と 平仮名のみで構成される単語が並んだ時は、
         漢字のみで構成される単語の候補として、送りがなありの単語を含めないこと。

[done] - カタカナ語が辞書に登録されないバグを修正する。
         "ー" 伸ばす記号が、平仮名語、カタカナ語に含まれている場合、単語に登録されない。

[done] - コーパスの読み込みを細切れのファイル単位で行えるようにする。


       - 行の折り返しで分割された単語をつなぎあわせる処理を行う。

[done] - 数値を見付けたら、数値型という単語として頻度登録する。

[done] - ふたつ離れた単語もテーブルに頻度登録する。(skip-bigram と呼ぶ?)

[done] - sumibi-tango->tango-id 以下を分かりやすく書き直す。 特に、その関数の中でなるべく判断を行う。

       - 辞書の内容を分析するためのツールを作成する。

       - 設定ファイル ~/.sumibi には、(make <sumibi-config> ...) の形式で定義できるようにする。

       - htmlファイルをコーパスとして扱えるようにする。

[done] - デバッグ表示モードをスイッチで制御できるようにする。

[done] - dbiutil.scm 側で、例外処理をいれたため、呼出し側から例外の guard処理を外す

       - ライブラリを (use ...) できるようにする。

[done] - プロトコルをSOAPに変更する。

[done] - WSDLファイルを作成する。

[done] - バグ修正:『修正』という単語が変換できない問題を修正する。(sumi_current2の場合のみ)

[done] - バグ修正: SKKの辞書から以下の行が読めていないようだ。
         しゅうせい /修正;(amend) 法案の-/集成/習性/修整;(retouch) 写真の-/終生/秋声/修成;修正し完成する/衆生/秋晴/終世;=終生/衆星/宗制/輯成/醜声/銹錆/

       - バグ修正:同一の語句を重複して辞書登録するバグを修正する。


   --- sumibi

       - xxx.methodでメソッドを指定できる様にする。
[done]   - xxx.h
[done]   - xxx.k
         - xxx.j
[done]   - xxx.l ( xxx.eも同じ意味に設定する )
         - xxx.L
         - xxx.num

       - xxx.methodの存在しないメソッドには反応しないようにする。

       - 変換候補の中に同じ候補が重複した場合は、削除して結果を返却する。

[done] - 結果を指定文字エンコードで返す

[done] - CGIからS式で返した結果が途中で切れる問題を解決する。→parseエラーで二度とEmacsから変換できないようになる。

[done] - ヘボン式以外のローマ字表記にも対応する。
[done]   - tye → チェ

       - 辞書の大きさ(レコード数など) を返すインターフェースをサポートする。

       - ユーザーが複数の辞書を切り替えて使えるようにする。

[done] - 入出力エンコードとして jis を追加サポートする。

[done] - she シェが入力できないのを修正する。


   --- sumibi.el
[done] - 変換結果の単語がアルファベット同士の場合は間のスペースを詰めない。

[done] - 変換候補の選択をサポートする。
          - 変換済み文字列にカーソルを合わせて[C-j]すると候補選択モードに移行する

[done] - インデントした場所から書きはじめた場合は、そのインデント位置を保持すること。

[done] - 候補選択のキャンセル[C-g]を実装する。

       - 変換中の色を"炭"と"火"の色(灰色とオレンジ色)にする。

       - 変換候補をミニバッファに表示する。

[done] - XEmacsに対応する。
[done]   - //nihongo という書式でエラーが出る。
[done]   - XEmacsは (overlay-put ov 'face 'normal) のノーマルのフェイスが存在しない。
[done]   - find-operation-coding-system 関数がXEmacsに存在しない。

[done] - (load "sumibi") だけで即使える様にする。 (global-sumibi-mode 1) は書かなくても良いようにする。

[done] - SSL証明書を sumibi.elのロード時に自動的に生成する。(インストール時に意識しなくてもよいようにする。)

[done] - no ga ha de など平仮名で書かれる頻度が非情に高いキーワードは .h メソッドをデフォルトとする。(カスタマイズ変数でサポートする)

       - ESC-j で漢字仮名交じりの文章からローマ字の文章に逆変換する。

[done] - curlが返すエラーメッセージを表示する機能を追加する。

[done] - Emacsの process-buffer の日本語エンコードを自動判別する。

[done] - バグ修正:サーバーから完全なS式が届かず、sumibi.elでパースエラーになる事がある。(Content-Length: 指定で解決する？)

       - curlが存在するか、とバージョンを起動時にチェックする。

[done] - 証明書なしのとき、curl に--insecure オプションを指定する。

       - 入力中のローマ字をリアルタイムで平仮名表示する。(平仮名ガイド)


    --- ドキュメント

[done] - FAQを追加する。

[done] - 対応Emacs/XEmacsバージョンを明記する。

[done] - 対応apelバージョンを明記する。

[done] - 動作実績のあるディストリビューションを明記する。

[done] - カスタマイズ変数の説明を追加する。

       - auto-fill-modeを使用している時は、前の行にさかのぼってローマ字変換する。

    --- sumi

[done] - SKK辞書から取り除きたい単語をSKKの辞書形式でCVS上でメンテする。


[EOF]
